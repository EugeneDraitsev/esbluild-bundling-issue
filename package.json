{
  "name": "esbuild-bundling-issue",
  "license": "MIT",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=out.js --platform=node --target=node14 --analyze"
  },
  "dependencies": {
    "telegraf": "4.4.2"
  },
  "devDependencies": {
    "esbuild": "^0.13.13",
    "lerna": "^3.22.1"
  }
}
